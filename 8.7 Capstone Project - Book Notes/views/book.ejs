<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= book_data.title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <!-- Back to Home Button -->
        <div class="mb-4">
            <a href="/" class="btn btn-secondary">Back to Home</a>
        </div>

        <% if (book_data) { %>
            <div class="row mb-4">
                <!-- Cover Image Column -->
                <div class="col-md-4 text-center">
                    <% if (book_data.coverUrl) { %>
                        <img src="<%= book_data.coverUrl %>" 
                             class="img-fluid rounded shadow" 
                             alt="Book Cover">
                    <% } else { %>
                        <p>No cover image available.</p>
                    <% } %>
                </div>
                <!-- Book Details Column -->
                <div class="col-md-8">
                    <h1 class="mb-3"><%= book_data.title %></h1>
                    <p><strong>Author:</strong> <%= book_data.author_name %></p>
                    <p><strong>Rating:</strong> <%= book_data.rating %></p>
                    <p><strong>Date Read:</strong> <%= book_data.date_read.getFullYear() %></p>
                    <p><strong>ISBN:</strong> <%= book_data.isbn %></p>
                </div>
            </div>
            <!-- Additional Content at the Bottom -->
            <div class="row">
                <div class="col-12">
                    <h3>About the Book</h3>
                    <p>
                        <%= book_data.notes %>
                    </p>
                </div>
            </div>
        <% } else { %>
            <p class="text-center text-danger">Book details not available.</p>
        <% } %>
    </div>
</body>
</html>