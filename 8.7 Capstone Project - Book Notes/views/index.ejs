<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Book Notes</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container">
        <h1 class="text-center mb-4">Book Notes</h1>
        <!-- I am passing a dictionary with JSON called 'book_data' and I want to loop through all keys and values -->
        <% if (book_data && book_data.coverUrl) { %>
            <div class="card mx-auto" style="width: 18rem;">
                <img src="<%= book_data.coverUrl %>" alt="Book Cover">
            </div>
        <% } %>
    </div>
</body>
</html>